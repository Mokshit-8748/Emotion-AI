# Legacy Quarantine

This folder stores superseded or broken experiments that are not part of the
current dashboard and production training flow.

Active training and inference paths stay in the project root:

- `train_finetune_3dataset.py`
- `src/train_model.py`
- `dashboard.py`

Quarantined files are kept here so they can be recovered later without
cluttering the main project surface.

